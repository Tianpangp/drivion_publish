# 发布系统原始需求

## 1. 背景与定位

Drivion 采用积木式项目组织方式，`publish` 是可独立交付、可按厂家需求选配的发布系统。发布系统负责现场资源、软件制品、版本流转、设备绑定和发布审计，不负责设备执行控制。

执行控制、AutoUnit 调度、HAL 调用、UI 运行、设备日志采集和现场运行细节属于 `drivion/exe`。发布系统只提供包管理、绑定管理、审批发布、拉取接口和自身操作日志。

## 2. 运行环境要求

- 后端 Python 环境使用 conda 的 `service` 环境。
- 不使用项目内 `venv` 作为后端运行环境。
- 发布系统保持独立 git 项目形态，避免与 Drivion 其他积木模块强耦合。
- 后端继续按当前 `publish-svc` 工程维护。
- 前端继续按当前 `publish-ui` 工程维护。

## 3. 名词与层级

统一现场层级：

```text
厂区 Factory > 线体 Line > 工位 Station > 设备 Equipment
```

- 厂区：客户现场的厂区、车间或生产区域。
- 线体：厂区下的生产线、单元线或工艺线。
- 工位：线体下的工位节点。
- 设备：工位下的具体设备实例，是 AutoUnit 绑定对象。

前端展示、接口命名和业务文案后续统一使用“设备”描述绑定对象；原有“工位”保留为上级现场结构，不再作为 AutoUnit 的最终绑定对象。

## 4. 核心业务边界

### 4.1 发布系统负责

- 厂区、线体、工位、设备档案管理。
- AutoUnit 包上传、版本管理、审批、状态流转、下载。
- HAL 驱动包上传、版本管理、审批、状态流转、下载。
- 设备与 AutoUnit 包的一对一绑定。
- AutoUnit 包被多个设备复用。
- 按设备提供 Exe Runtime 拉取所需的包信息。
- 发布系统自身操作日志、审计、保留和分割。
- 多角色权限与审批流程。

### 4.2 发布系统不负责

- 不控制设备。
- 不实现 AutoUnit 执行顺序、入口调度、运行状态机。
- 不处理设备运行日志数据。
- 不直接采集设备状态。
- 不维护 UI 界面包。
- 不维护部署清单。
- 不做离线导入/导出发布包。
- 不做多厂家、多项目、多租户隔离。

### 4.3 与 Exe 的边界

- DevKit 负责开发、验证和导出 AutoUnit 包。
- Runtime 负责从 Publish 拉取、安装、激活、回滚和运行包。
- Publish 负责接收包、管理版本、审批发布、维护设备实例绑定。
- AutoUnit 包携带 `drivion.project.json`，发布系统至少读取其中的 `name`、`package_id`、`version`、`module`。
- 设备拉取时按包名、包 ID、版本等信息获取绑定包和下载信息。

示例 `drivion.project.json`：

```json
{
  "name": "自动化逻辑示例设备",
  "package_id": "sample-automation-logic",
  "version": "0.1.0",
  "module": "sample_automation_logic"
}
```

## 5. 现场资源管理

### 5.1 厂区管理

- 支持厂区创建、编辑、停用、启用、软删除、查询。
- 支持厂区编号、名称、位置、描述、状态等基础字段。
- 厂区下挂载线体。

### 5.2 线体管理

- 支持线体创建、编辑、停用、启用、软删除、查询。
- 支持线体编号、名称、描述、状态等基础字段。
- 线体必须归属厂区。
- 线体下挂载工位。

### 5.3 工位管理

- 支持工位创建、编辑、停用、启用、软删除、查询。
- 支持工位编号、名称、IP、MAC、描述、状态等基础字段。
- 工位必须归属线体。
- 工位下挂载设备。

### 5.4 设备管理

- 支持设备创建、编辑、停用、启用、软删除、查询。
- 支持设备编号、名称、类型、厂商、型号、描述、状态等基础字段。
- 设备必须归属工位。
- 设备是 AutoUnit 包绑定对象。
- 支持按厂区、线体、工位、设备编号、设备名称、设备类型、设备状态筛选。
- 支持设备迁移到其他工位。
- 支持查看设备当前绑定的 AutoUnit 包、绑定历史、最近更新时间。

## 6. AutoUnit 包管理

### 6.1 上传与解析

- 支持上传 AutoUnit 包文件。
- 上传后默认进入“未转测”状态。
- 包名、包 ID、版本需要唯一。
- 上传时读取 `drivion.project.json` 的必要信息。
- 必要信息包括 `name`、`package_id`、`version`、`module`。
- 不做入口函数校验。
- 不做完整执行逻辑校验。
- 支持记录文件名、大小、MD5、SHA256、上传人、上传时间。
- 支持记录 README、变更说明、依赖信息和 Python 版本信息。

### 6.2 版本管理

- 支持按包 ID 查看所有版本。
- 支持查看版本详情。
- 支持下载指定版本。
- 支持版本状态流转。
- 已发布版本不做物理删除。
- 已绑定设备且设备正在使用的版本不能下架。
- 下架后可将设备绑定切换到新版本。

### 6.3 设备绑定

- AutoUnit 绑定到具体设备。
- 一个设备只能绑定一个 AutoUnit 包版本。
- 一个 AutoUnit 包版本可以被多个设备使用。
- 绑定允许选择任意版本。
- 绑定未发布版本时，前端必须标红提示。
- 支持设备侧查看当前绑定包。
- 支持包版本侧查看已绑定设备列表。
- 支持解绑、替换版本和绑定历史。

## 7. HAL 驱动包管理

HAL 是驱动包，不绑定设备。AutoUnit 内部通过 `hal.get()`、`hal.create()` 等方式使用 HAL 驱动，执行细节由 Exe 负责。

发布系统只负责 HAL 驱动包上传和版本管理：

- 支持上传 HAL 驱动包。
- 上传后默认进入“未转测”状态。
- 支持驱动包名、包 ID、类型、版本唯一性校验。
- 支持记录协议、厂商、型号、支持平台、API 文档、README、校验值。
- 支持按包 ID 查看所有版本。
- 支持查看版本详情。
- 支持下载指定版本。
- 支持审批、驳回、发布、下架。
- 不做设备绑定。
- 不做驱动实例配置。
- 不做 HAL 调用链校验。

## 8. 包状态与审批流

### 8.1 主状态

包状态统一按业务语义展示：

- 未转测：包已上传，尚未进入测试。
- 已转测：包已通过转测动作，可用于测试或试用。
- 已发布：包允许作为正式版本绑定和拉取。
- 已驳回：审批或转测失败，需要重新上传或重新提交。
- 已下架：不再允许新绑定和新拉取。

当前代码中的状态可逐步映射：

- `unpublished` -> 未转测。
- `testing` -> 已转测。
- `published` -> 已发布。

后续需要补齐 `rejected`、`unpublished/removed` 等更稳定的枚举语义。

### 8.2 审批动作

AutoUnit 包和 HAL 驱动包都需要审批流：

- 上传。
- 提交转测。
- 转测通过。
- 转测驳回。
- 提交发布。
- 发布通过。
- 发布驳回。
- 下架申请。
- 下架通过。
- 下架驳回。

### 8.3 下架规则

- 已绑定设备且设备正在使用的包版本不能下架。
- 下架版本不允许新增绑定。
- 下架版本不作为默认可拉取版本。
- 设备从下架版本切换到新版本后，旧版本可继续保留历史记录。

## 9. 权限角色

需要实现多角色权限，至少覆盖：

- 管理员：用户、角色、权限、全部资源和全部审批动作。
- 发布管理员：包上传、转测、发布、下架、设备绑定管理。
- 测试人员：转测审核、转测驳回、查看包详情。
- 工程人员：设备档案维护、设备绑定、查看包信息。
- 查看人员：只读查看设备、包、绑定和日志。

关键动作必须受权限控制：

- 创建、编辑、删除厂区/线体/工位/设备。
- 上传 AutoUnit 包。
- 上传 HAL 驱动包。
- 提交转测、转测通过、转测驳回。
- 提交发布、发布通过、发布驳回。
- 下架申请、下架通过、下架驳回。
- 设备绑定、解绑、替换版本。
- 日志查看和日志导出。

## 10. 设备状态

设备状态由执行平台上报到 MES，发布平台后续从 MES 获取。MES 当前未实现，发布平台暂不实现设备状态采集和设备状态接口。

发布系统当前只保留设备档案中的管理状态，例如启用、停用、删除。设备真实在线、离线、运行、异常等运行态后续再接 MES。

## 11. 日志管理

发布系统只处理自己的操作日志和审计日志，不处理设备运行日志、AutoUnit 执行日志或 HAL 运行日志。

操作日志要求：

- 记录操作人、角色、时间、IP、请求路径、请求方法。
- 记录操作对象、对象类型、操作动作、操作结果。
- 记录变更前后关键信息。
- 记录失败原因。
- 支持按时间、操作人、模块、动作、结果、对象查询。
- 支持导出。
- 默认保留一个月。
- 单个日志文件按 100MB 分割。

## 12. Exe Runtime 拉取

设备拉取时基于设备绑定关系获取当前 AutoUnit 包版本。

拉取接口需要返回：

- 设备信息。
- 所属厂区、线体、工位路径。
- 当前绑定 AutoUnit 包的 `name`、`package_id`、`version`、`module`。
- 下载地址。
- 文件大小。
- MD5、SHA256。
- 状态。
- 上传时间、发布时间。
- 依赖信息。
- 必要的参数契约和硬件配置占位信息。

绑定未发布版本时，管理端必须标红提示；拉取接口是否允许返回未发布版本由绑定策略控制。默认情况下，正式运行设备应拉取已发布版本。

## 13. 去除模块

当前旧实现中的以下模块不再进入发布系统主流程：

- 界面包管理。
- 界面与工位绑定。
- 部署清单管理。
- 部署清单与工位绑定。

后续实现可以保留旧代码过渡，但新需求和新 UI 不再围绕这些模块扩展。

## 14. 当前实现与需求差距

- 当前已有厂区、线体、工位三级模型，缺少设备模型。
- 当前 AutoUnit 绑定到工位，需要改为绑定到设备。
- 当前 AutoUnit 支持一个包应用到多个工位，但缺少“一个设备只能绑定一个 AutoUnit 版本”的约束。
- 当前驱动包仍展示工位绑定概念，需要移除 HAL 绑定设备的业务。
- 当前状态枚举为 `unpublished/testing/published`，缺少驳回、下架、审批流。
- 当前下架规则没有校验设备是否正在使用。
- 当前操作日志是数据库审计日志，缺少一个月保留和 100MB 分割策略。
- 当前 Exe 聚合接口按工位返回清单、AutoUnit、驱动、界面，需要调整为按设备返回绑定 AutoUnit 包信息。
- 当前 README 中的界面包和部署清单描述需要后续同步清理。
- 当前前端菜单和文案仍存在“工位绑定、界面、部署清单”等旧概念，需要逐步调整为设备主线。

## 15. 初步优先级

### P0：主数据和绑定链路

- 新增设备模型。
- 建立厂区、线体、工位、设备四级结构。
- AutoUnit 包绑定对象从工位改为设备。
- 实现一个设备只能绑定一个 AutoUnit 包版本。
- 移除 HAL 与设备绑定的需求和入口。
- 前端文案统一使用设备。

### P1：包状态和审批

- AutoUnit 包状态补齐未转测、已转测、已发布、已驳回、已下架。
- HAL 驱动包状态补齐未转测、已转测、已发布、已驳回、已下架。
- 实现转测审批、发布审批、下架审批。
- 实现已绑定且正在使用版本不可下架。
- 绑定未发布版本时前端标红。

### P2：Exe 拉取与审计

- 按设备提供 Exe Runtime 拉取接口。
- 返回 `drivion.project.json` 关键字段和下载校验信息。
- 补齐绑定历史、状态流转历史、审批历史。
- 补齐操作日志查询、导出、一个月保留、100MB 分割。

### P3：旧模块清理

- 从新 UI 中移除界面包管理。
- 从新 UI 中移除部署清单管理。
- 清理旧 README 和 API 文档中过时描述。
- 保留必要兼容接口，直到确认没有依赖方使用。
